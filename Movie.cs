using System;

namespace Task
{
    class Movie
    {
        public void MovieTicket()
        {
            /*
            int age;
            string show;
           
            Console.WriteLine("Enetr the age :");
            age = int.Parse(Console.ReadLine());
            Console.WriteLine("Enetr the show :");
            show  = Console.ReadLine();

            if (age >= 65 && show=="morning")
            {
               
            }
            */
        }
    }
}
