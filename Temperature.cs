using System;


namespace Task
{
    class Temperature
    {
        public void TempConversion()
        {
            /*
            int choice;
            Console.WriteLine("To convert Farahneit to celcius press 1");
            Console.WriteLine("To convert celcius to Farahneit  press 2");
            choice  = int.Parse(Console.ReadLine());

            if (choice != 1 && choice != 2)
            {
                Console.WriteLine("Enetr the correct choice:");
                choice = int.Parse(Console.ReadLine());
            }
            Console.WriteLine("Enetr the Temp value");
            double Temp = double.Parse(Console.ReadLine());

            switch(choice)
            {
                case 1:
                    Temp = (Temp - 32) * 5 / 9;
                    Console.WriteLine("The temperature in celcius : " +Temp);
                    break;
                case 2:
                    Temp = Temp * 9 / 5 + 32;
                    Console.WriteLine("The temperature in faraneit :" + Temp);
                    break;
            }
            */

        }

        
    }
}
